import { StyleSheet } from "react-native";

export const styles =StyleSheet.create({
  container: {
    zIndex: 999,
    position: "absolute",
    alignSelf: "center",
    bottom: 200
  },
  canvas: {
    width: 150,
    height: 260,
  }
})
